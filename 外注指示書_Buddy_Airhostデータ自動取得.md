# 業務委託 指示書: Airhostデータ取得の自動化（Chrome拡張 → Buddy 移管）

| 項目 | 内容 |
|---|---|
| 発注者 | Dent Inc.（石井 元 / gen_ishii@dent-inc.com） |
| 受注先 | mybrain.tv（三口 聡之介 様） |
| 対象 | buddy AIワーカー（Mac mini 常駐側） |
| 作成日 | 2026-08-10 |
| 経緯 | 2026-08-10 定例MTでの合意事項（「毎日8時に取りに行ってD1に保存」） |
| 状態 | **仕様確定・Dent側実装済**。保存の口とトークンは用意できています（§8）。Buddy側の実装可否とご意見をお待ちしています |

---

## 1. 依頼の要旨

現在 Airhost の **予約データ / 日次データ** は、石井が毎朝 Chrome拡張のボタンを
手で押して取得しています（土日含めて毎日、所要5分）。

これを **Buddy 側で自動実行** していただきたい、というのが依頼です。
**2種類のジョブ**になります（詳細は §3）。

| ジョブ | 頻度 | 目的 |
|---|---|---|
| **A. 差分取得** | **毎時** | 新しく入った予約をすぐ反映する |
| **B. 全量リコンサイル** | **1日1回（朝8:00 JST）** | 取りこぼし・キャンセルを含めて整合させる |

やることは3つです（A・Bとも同じ）。

1. Airhost の内部エクスポートAPIを叩いて CSV を取得する（§4）
2. CSV をパースし、**ゲスト個人情報を落として**（§6）JSON化する
3. Dent の受け口 `POST /cs/ingest/*` に投げる（§5）

**保存の口とトークンは用意済みです。**（MTで「保存の口を全部用意してくれれば」
「アクセス権限だけ与えたAPIキーをくれれば」とおっしゃっていた分です。）

Airhost 側の呼び出し手順も Chrome拡張の実装で確立済みなので、
**そのまま移植していただければ動きます**。本書はその全手順を記載したものです。

---

## 2. 背景（なぜ移管するのか）

### 現状のフロー

```
[人間が毎朝ボタンを押す]
    ↓
Chrome拡張 (Airhostのログインセッションを借りて内部APIを叩く)
    ↓
Google Sheets（直近90日ぶんのバッファ）
    ↓
GAS d1SyncDaily
    ↓
Cloudflare D1（全履歴を永続蓄積）
    ↓
運営ダッシュボード / 朝の運営サマリー / 民泊新法の営業日数チェック / 異常検知
```

### 問題

- **人間が押さないと止まる。** 押し忘れた日はダッシュボードも朝レポートも前日のまま
- **押す人が1人しかいない。** 石井が不在の日に代替がいない
- Google Sheets は「D1に入れるまでの中継バッファ」でしかなく、
  **本来なくてよい経由地**（拡張が直接D1に書けない事情があるだけ）

Buddy は既に Mac mini 上で Airhost のセッションを保持して常駐しているので、
**この処理を持つのに一番適した場所**です。

### 移管後のフロー

```
[Buddy] A. 毎時（差分・新規予約）      B. 毎朝8:00（全量リコンサイル）
              ↓                              ↓
        Airhost 内部API（§4）          Airhost 内部API（§4）
              ↓                              ↓
        CSV → JSON（PII除去）          CSV → JSON（PII除去）
              ↓                              ↓
        POST /cs/ingest/*  ────┬────  POST /cs/ingest/*      （§5）
                               ↓
                        Cloudflare D1（upsert）
                               ↓
                    運営ダッシュボード（常に最新）
                               ↓
                    B の後だけ完了フックを1回
                    → 朝レポート＋新法通知が発火          （§7）
```

**Google Sheets 経由が丸ごと不要になります。**

---

## 3. 取得する2種類のデータと、2つのジョブ

### 3.1 データの種類

| 種別 | 中身 |
|---|---|
| **予約データ** | 予約1件＝1行。チェックイン/アウト、泊数、人数、国籍、金額内訳、ステータス |
| **日次データ** | 「物件×部屋×日付」＝1行。その日に計上された売上・清掃料・税等 |

MTで「予約データが2種類ある」と話に出たのはこの2つです。同じ予約を
別の切り口で見たもので、**日次データの方が Airhost の画面には出ていない粒度**まで持ちます。
両方必要です。

### 3.2 ジョブB: 全量リコンサイル（1日1回・朝8:00 JST）

| 種別 | `date_type` | 取得範囲 | チャンク数 |
|---|---|---|---|
| 日次データ | —（指定なし） | 過去90日 〜 当日 | 1 |
| 予約データ | `checkin_date` | 過去90日 〜 未来365日 | 5 |

確定した過去の予約は変わらないため、それより前は取り直しません。
全履歴は D1 側が upsert で永続保持しています（2023年1月〜現在まで投入済み）。

**過去90日は下限です。** これより短くしないでください。Dent側の異常検知バッチが
直近90日のADR平均をベースラインに使っているためです。

### 3.3 ジョブA: 差分取得（毎時）★今回の追加要件

**目的は「新しく入った予約をすぐ反映する」ことだけ**です。全量を毎時取る必要はありません。

```jsonc
POST /api/one/pms/bookings/export?locale=ja
{
  "date_range": { "start_date": "<当日>", "end_date": "<当日>" },
  "date_type": "updated_at",     // ← §4.6 の検証結果によって決めます
  "status": []
}
```

**エクスポート1回で済みます。**日次データは毎時取らなくて構いません
（過去日の売上は時間単位では動かないため。ジョブBで拾えます）。

#### なぜ `checkin_date` のままではいけないか

現在の取得は**チェックイン日**で窓を切っています。この切り方だと、
「今日入った、8ヶ月先に泊まる予約」を拾うために**未来365日ぶん全部**を
取り直す羽目になります。毎時これをやると1日144回のエクスポートになり、
Airhost 側でブロックされるリスクが現実的になります。

**予約日／更新日で切れば、チェックイン日が何ヶ月先でも当日分1回で拾えます。**

#### 取りこぼしについて（重要）

`date_type` に何が使えるかで、毎時で拾える範囲が変わります。

| 使える値 | 新規予約 | 変更 | キャンセル |
|---|---|---|---|
| `updated_at` | ✅ | ✅ | ✅ |
| `booking_date` | ✅ | ❌ | ❌（予約日が過去なので窓に入らない） |

`booking_date` しか通らない場合、**過去に入った予約のキャンセルは毎時では拾えず、
翌朝のジョブBで反映**されます（最大1日遅れ）。売上・OCCは朝に必ず整合するので
実用上は問題ありませんが、`updated_at` が通るならそちらを使ってください。

**まず §4.6 の検証をお願いします。**

### 3.4 順序は問いません

Dent側は `INSERT OR REPLACE` の upsert です。ジョブAとジョブBがどの順で届いても、
同じデータが何度届いても、結果は同じになります。**毎時の部分取得と朝の全量取得は
勝手に噛み合います。**失敗した回を気にせず次に進んで構いません。

---

## 4. Airhost からの取得手順

### 4.1 エンドポイント

```
ベース: https://api2.airhost.co

日次データ: POST /api/one/pms/booking_sales_raw_data/export?locale=ja
予約データ: POST /api/one/pms/bookings/export?locale=ja
```

**リクエストボディ**

```jsonc
// 日次データ
{ "date_range": { "start_date": "2026-05-12", "end_date": "2026-08-10" } }

// 予約データ
{
  "date_range": { "start_date": "2026-05-12", "end_date": "2027-08-10" },
  "date_type": "checkin_date",
  "status": []
}
```

**リクエストヘッダ**

```
Content-Type:        application/json
Accept:              application/json
X-User-Os-Timezone:  Asia/Tokyo
X-Ah-Tenant:         <テナントUUID>       ← 必須
X-CSRF-Token:        <CSRFトークン>       ← 実質必須
Cookie:              <ログインセッション>  ← credentials: 'include' 相当
```

### 4.2 認証情報の取り方

これは Airhost の**公開APIではなく内部API**です。ログイン済みブラウザ
コンテキストから叩く前提の作りになっており、公式なAPIキーはありません
（プランはPro。Airhost の "Premium API" とは別物です）。

拡張では以下の方法で取得しています。Buddy が Playwright 等でブラウザを
持っているなら同じ手が使えます。

| 情報 | 取得方法 |
|---|---|
| **テナントUUID** (`X-Ah-Tenant`) | ① WebSocket 送信メッセージ内の `/accounts/<UUID>` を傍受 ② `__NEXT_DATA__` の JSON から正規表現 ③ localStorage / sessionStorage / Cookie / インラインscript を横断検索 — のいずれか。拡張はこの順で試します |
| **CSRFトークン** (`X-CSRF-Token`) | ① ページの XHR が `setRequestHeader('X-Csrf-Token', …)` する値を傍受 ② `GET /api/one/pms/preflights?locale=ja` のレスポンスヘッダ/ボディ ③ Cookie の `XSRF-TOKEN` |

**⚠️ 401 が返ったら本文を見てください。** `{ "data": { "csrf_token": "…" } }` の形で
新しいトークンが同梱されて返ってくることがあります。それに差し替えて
500ms 待って再試行すれば通ります。拡張は最大2回リトライしています。

### 4.3 ジョブ完了待ち（ここが一番の落とし穴）

POST は CSV を直接返しません。**非同期エクスポートジョブが立ち上がり、トピック名だけが返ります。**

```jsonc
// POST のレスポンス
{ "topic": "/airhost_booking_sales_raw_data_export_jobs/<JOB_UUID>" }
```

完了通知は **WebSocket (ActionCable)** で来ます。ポーリングのエンドポイントは
見つかっていません。手順は以下です。

```jsonc
// ① MessageQueueChannel に subscribe
{ "command": "subscribe",
  "identifier": "{\"channel\":\"MessageQueueChannel\",\"nonce\":\"<任意のUUID>\"}" }

// ② 500ms ほど空けてから、トピックを subscribe
//    ⚠️ トピック名の末尾に "/result" を足すこと
{ "command": "message",
  "identifier": "{\"channel\":\"MessageQueueChannel\",\"nonce\":\"<同じUUID>\"}",
  "data": "{\"topic\":\"<topic>/result\",\"afterSid\":\"0\",\"action\":\"subscribe_topic\"}" }
```

完了メッセージは入れ子になっています。`message.records[].body`（**文字列なので
もう一度 JSON.parse が要ります**）の中の `object.files[0].url` が CSV のURLです。

```
message.records[N].body (string)
  → JSON.parse
  → .object.files[0].url    ← これが欲しいURL
```

`blob_id` / `download_url` / `url` が直下に来るパターンもあるため、拡張では
両方見ています。

**注意点**

- タイムアウトは **300秒**（データ量によっては数十秒かかります）
- **待機中に WebSocket が切れて再接続されることがあります。** 再接続を検出したら
  **subscribe をやり直してください。** これをやらないと完了通知を取り逃してタイムアウトします
- ジョブUUID と Blob UUID は**別のID**です

### 4.4 CSV のダウンロード

`blob_id` しか得られなかった場合は `GET https://api2.airhost.co/blobs/<BLOB_UUID>`。
これは **302 で CDN の署名付きURL にリダイレクト**します
（`https://one-cdn.airhost.co/private/<UUID>/RawData_xxx.csv?Signature=…&Expires=…`）。

リダイレクト先は **Cookie 不要**（署名で認可済み）なので、素直に follow して
本文を取得してください。

CSV は先頭に **BOM (`﻿`)** が付きます。パース前に除去してください。

### 4.5 レート制限とチャンク分割

Airhost のエクスポートは **1回あたり3ヶ月が上限**です（UI上の制限）。
また連続で叩くとレート制限に当たります。拡張では以下で運用しています。

- 期間を **3ヶ月単位のチャンク**に分割
- **各エクスポートの間に35秒待つ**（チャンク間・日次↔予約の切替時とも）

**毎時のジョブAは1エクスポートで完結する**ので、この待機は発生しません。

過去90日＋未来365日を取ると、日次1チャンク＋予約5チャンク＝計6回。
**待機込みで全体10〜15分**かかります。8:00開始なら8:15には終わる想定です。

（この待機時間はブラウザ拡張での経験則です。Buddy 側で短縮できるなら
短くして構いませんが、429/5xx が出るようなら戻してください。）

#### 想定される負荷

| 構成 | エクスポート回数/日 |
|---|---|
| 現在（人が1日1回ポチ） | 6回 |
| **今回の提案（毎時A ＋ 朝1回B）** | **30回**（毎時24 ＋ 朝6） |
| 参考: 毎時に全量を取った場合 | 144回 ← **避けたい** |

これは公開APIではなく**Airhostの内部API**で、レート制限の定義も契約もありません。
止められると予約管理そのものが止まるため、**回数は必要最小限に寄せています**。

### 4.6 ⚠️ 実装前に検証していただきたい2点

どちらも `date_range` / `date_type` を変えて1回叩くだけで分かります。
**結果によって設計が変わる**ので、着手の最初にお願いします。

#### ① `date_type` が受け付ける値

現在判明しているのは `checkin_date` のみです。以下を試してください。

| 試す値 | 通った場合の意味 |
|---|---|
| `updated_at` | **最良**。毎時のジョブAで新規・変更・キャンセルを全部拾えます |
| `booking_date` | 新規予約は拾えます。キャンセルは翌朝のジョブB待ちになります |
| `checkout_date` | 参考情報として |

**どれも通らず `checkin_date` しか使えない場合はご連絡ください。**
その場合は毎時の窓を「過去3日＋未来120日」に切る案に切り替えます
（未来120日より先の新規予約が最大1日遅れます）。

#### ② 3ヶ月上限がAPIにもあるか

`date_range` に**12ヶ月**を入れて通るか試してください。UIだけの制限であれば、
朝のジョブBが予約5チャンク→1チャンクになり、**1日6回→2回**まで減らせます。

---

## 5. Dent への送信

### 5.1 エンドポイント

**ベースURLは抑制連携でお使いいただいているものと同じ**です
（2026-07-17 のご返信で確定した `dent-data-api.gen-ishii.workers.dev`）。

```
POST https://dent-data-api.gen-ishii.workers.dev/cs/ingest/reservations
POST https://dent-data-api.gen-ishii.workers.dev/cs/ingest/daily-revenue
```

`api.dent-inc.com` は Cloudflare Access 配下で、`/cs/*` の Bypass 設定が
まだ入っていないため**そちらは使えません**（ログイン画面に弾かれます）。
整備できたらご案内しますが、**移行はベースURLの差し替えだけ**です。

### 5.2 認証

レビュー抑制連携でお渡ししているものと**同系統の Bearer トークン**です。

```
Authorization: Bearer <お渡しします>
Content-Type:  application/json
```

社内API `/internal/*` のトークンはお渡ししません。あちらには Airbnb の
セッション情報・2FAコード・マスタ編集など無関係かつ機微なものが同居しているためです
（前回の指示書 §4 と同じ理由です）。

#### トークンは既存のものをそのまま使えます

**レビュー抑制連携でお渡し済みのトークンに、取り込み権限を追加します。**
新しいトークンの発行はありません。**Buddy側の設定変更は不要**です。

内部的にはトークンにスコープ（`exclusions` / `read` / `ingest`）を持たせており、
お渡ししているトークンは3つ全部を持つ形にします。

なお `/internal/*` との分離は従来通りです。このトークンで叩けるのは `/cs/*` だけで、
Airbnb のセッション情報・2FAコード・マスタ編集には到達しません。

（403 が返る場合はスコープ不足です。レスポンスに不足しているスコープ名が入ります。
再発行は不要なのでご連絡ください。）

### 5.3 ボディ

**チャンクあたり200行**で分割して POST してください（D1のバッチ制限のため）。
**1リクエスト1000行を超えると 413 で弾きます**（Worker のメモリ上限に当てて
5xx を返すより、理由が伝わる形で落とした方がよいため）。

```jsonc
{ "rows": [ { …1行 }, { …1行 }, … ] }
```

各行は **CSVの日本語ヘッダをそのままキーにしたオブジェクト**です。
列名は §6 の通り。**存在しない列は送らなくて構いません**（null 扱いになります）。

#### ⚠️ 日次データだけ `_raw` を併せて送ってください

日次データのCSVには **`含まれる` という同名の列が9個** あります。
素直にオブジェクト化すると衝突して潰れます。

そのため日次データの行だけ、**キー付きオブジェクトに加えて `_raw`（CSVの列順そのままの配列）を
入れてください。** Dent側は金額系を `_raw` の位置から読みます。

```jsonc
{
  "rows": [{
    "_raw": ["ドムス桃谷", "101", "MMK101", "20248543", "確認済み", "2026-08-01",
             "JPY", "18000", "Airbnb", "16200", "5000", "true", "", "", …],
    "物件名": "ドムス桃谷",
    "ルーム番号": "MMK101",
    "日付": "2026-08-01",
    "予約番号": "20248543"
  }]
}
```

`_raw` は **CSVの列順を1文字も動かさずに**入れてください。並びが変わると
金額が別の列として保存されます（エラーにはならず、静かに壊れます）。

キー側は最低限 **`物件名` / `ルーム番号` / `日付` / `予約番号`** の4つが必要です
（この4つは `_raw` ではなくキーから読んでいます）。

予約データの方は同名列がないので `_raw` は不要、キー付きオブジェクトだけで結構です。

### 5.4 レスポンス

```jsonc
{
  "accepted": 200,       // 受け取った行数
  "upserted": 198,       // 実際に書いた行数（重複IDは1件に寄せる）
  "changes": 198,
  "orphans": 3,          // 物件マスタに紐付かなかった行数
  "orphan_samples": [ { "property_name": "…", "room_no": "", "ref": "20248543" } ],
  "notes": [ "…" ]       // 警告がある場合のみ。§5.4「notes」参照
}
```

**`notes` が付いていたら必ずログに残してください。** エラーではないが
静かにデータが欠ける状態を知らせるものです。今のところ2種類出ます。

| notes の内容 | 意味 |
|---|---|
| `個人情報の列を破棄しました: …` | §6.2 のPII列が混ざっていました。Dent側で捨てているので実害はありませんが、送信前に落としてください（`ゲスト名` は §6.2.1 のとおり2026-08-31から**送信対象**です。破棄対象ではありません） |
| `N行に _raw がありません` | §5.3 の `_raw` が無いため、「含まれる」フラグが保存されていません。**金額そのものは入りますが内訳の判定が落ちます** |

- **冪等です。** 同じデータを何度送っても `INSERT OR REPLACE` で上書きされるだけです。
  失敗したチャンクは丸ごと再送してください
- **`orphans` は通常2%程度出ます（正常）。** Airhost が部屋番号を空で吐く予約が原因で、
  ほぼキャンセル系です。**ゼロにする必要はありません**。ただし急に増えた場合は
  物件マスタ側の問題なのでご連絡ください
- HTTP 200 以外は失敗です。ボディに理由が入ります

### 5.5 予約の「書き戻し」はできません

前回の指示書と同じですが、**Dent側に予約内容を書き込むAPIは意図的に用意していません。**
D1 の予約データは Airhost のミラーで、毎朝洗い替えられます。
予約内容を変えたい場合は Airhost 側を直接更新してください。

---

## 6. 列の定義とPIIの扱い

### 6.1 予約データ（CSV 42列）

Dent が保存するのは以下の列です。**この表にない列は送らないでください。**

```
予約サイト / チャンネル予約ID / AirHost予約ID / チェックイン / チェックアウト /
物件名 / 物件タグ / 部屋番号 / 国籍 / ゲスト名 / 状態 / チェックイン状態 / 支払い済み /
ゲスト数 / 合計日数 / 予約日 / 通貨 / 販売 / OTA サービス料 / 受取金 /
支払済み（補助金/クーポン） / OTA 決済 / クレジット / 現金 / 未収 /
クリーニング代 / サポート料金 / 料金プラン / 大人 / 子供 / 幼児 /
決済手数料 / キャンセル / 予約エンジンクーポン / 更新日時 / ルームタイプメニュー
```

`AirHost予約ID` が主キーです。**これが空の行はスキップしてください。**

### 6.2 🔴 除外必須（個人情報）

CSV には以下の列が含まれますが、**Dent の D1 には保存しません。送信前に落としてください。**

```
電話番号 / メールアドレス / ゲストアドレス / コメント / 説明
```

**設計上の決定であり、運用ルールではありません。** ダッシュボードも朝レポートも
これらを一切参照していません（確認済み）。個人情報は Airhost / Buddy 側に留め、
分析基盤には持ち込まない、という切り分けです。

**Dent 側でも受信時に破棄する実装を入れました**（混ざっていたら `notes` で
お知らせします）。ただし送信経路とログに載る時点で持ち込みたくないので、
**Buddy 側でも落としてください。** 二重の防御です。

### 6.2.1 ⚠️ 2026-08-31 変更: `ゲスト名` は送ってください

**以前は除外必須でしたが、`ゲスト名` だけ §6.1 の送信対象に移しました。**
オーナーが新法（住宅宿泊事業法）の**宿泊者名簿**を作るのに必要という要望を受けての
変更です。`宿泊者名` 列がある場合も、`ゲスト名` が空のときの代わりとして使います。

- 送るのは **`ゲスト名`（と `宿泊者名`）だけ**です。
  **電話番号・メールアドレス・住所・コメントは引き続き送らないでください。**
- Dent 側では氏名を **13ヶ月で自動削除**し、オーナー本人だけが自分の予約分を
  画面から都度取得できる形にしています（明細ファイルには保存しません）。

対応前でも取り込みは通ります（氏名が空のまま入るだけ）ので、**急ぎではありません。**
対応いただける時期だけ教えてください。

### 6.3 日次データ（CSV 28列・この順序が `_raw` の並び）

```
 0 物件名              1 部屋名              2 ルーム番号          3 予約番号
 4 状態                5 日付                6 通貨                7 売上合計
 8 予約サイト          9 受取金合計         10 清掃料             11 含まれる
12 ペナルティ料金     13 含まれる           14 その他の費用       15 含まれる
16 追加人数の料金     17 含まれる           18 Airhost ホテル商品 19 含まれる
20 POSサービス        21 含まれる           22 内税               23 含まれる
24 外税               25 含まれる           26 料金調整           27 含まれる
```

日次データに個人情報の列はありません。

`物件名` と `日付` のどちらかが空の行はスキップしてください。
`物件名+ルーム番号+日付+予約番号` の組が重複する行は先勝ちで1件に寄せます。

---

## 7. 完了後のフック

### 🔴 これは「朝のジョブB」の後だけです。毎時のジョブAでは叩かないでください。

朝の全量リコンサイル（ジョブB）が終わったら、**最後に1回だけ** GAS の以下URLを
GET してください。

```
GET <GAS_WEBAPP_URL>?action=reportOnly&token=<お渡しします>
```

これで **朝の運営サマリー（#management へ投稿）** と
**民泊新法の営業日数チェック通知** が、最新データで発火します。

- **同日に複数回叩いても本編の投稿は1回だけ**です（GAS側に冪等ガードあり）。
  つまり毎時叩いても事故は起きませんが、**実行ログが無駄に汚れて障害調査の邪魔になる**ので
  朝だけにしてください
- 失敗しても ingest 自体には影響しません。レスポンスの `ok` / `shinpou` を
  ログに残していただけると助かります

### `morningReport` ではなく `reportOnly` です

似た名前の `action=morningReport` が既にありますが、そちらは Chrome拡張用です。
投稿の前に **Sheets→D1 の取り込み** を走らせます（拡張は Sheets にしか書けないため）。

`reportOnly` はその取り込みを飛ばして投稿だけ行う、Buddy 用の入口です。**実装済みです。**

**万一 `morningReport` を叩いてしまっても壊れないようにしてあります。**
Dent側の取り込み処理に「シートが古ければ取り込まない」ガードを入れました。
Buddy が D1 に直接書くようになると Sheets は更新されなくなるので、
そのシートで D1 を上書きしてしまう事故（エラーが出ず数字だけ巻き戻る）を
呼び出し方に依存せず防いでいます。**とはいえ `reportOnly` をお使いください。**

---

## 8. Dent側の実装状況

| 項目 | 状態 |
|---|---|
| D1 テーブル（`reservations` / `daily_revenue`） | ✅ 稼働中 |
| ingest ロジック（列マッピング・物件コード解決・upsert） | ✅ 稼働中 |
| **`/cs/ingest/reservations` / `/cs/ingest/daily-revenue`** | ✅ 実装済（§5） |
| **既存トークンへの `ingest` 権限の付与** | ✅ 対応済（§5.2・Buddy側の設定変更なし） |
| **受信時のPII破棄 ＋ `_raw` 欠落の警告** | ✅ 実装済（§5.4 / §6.2） |
| **`reportOnly`（`d1SyncDaily` を走らせない完了フック）** | ✅ 実装済（§7） |
| **古いSheetsでD1を上書きしないガード** | ✅ 実装済（§7） |
| 異常検知バッチ (`alert-anomaly.gs`) を Sheets読み → D1読みへ移行 | ⬜ Dent側で対応中 |

**Buddyから投げていただければ、その時点でD1に入る状態まで出来ています。**

最後の1つだけ残っています。Sheets を読んでいる処理が**異常検知バッチだけ**残っており、
これを D1 読みに変えないと Sheets を完全に止められません。**Dent側の作業**で、
**Buddy 側の実装をブロックするものではありません**（§9 の段階1〜3は先に進められます）。
Sheets 経路の撤去（段階4）だけがこれ待ちです。

---

## 9. 段階的な移行の進め方（提案）

いきなり切り替えると、失敗した日にダッシュボードが丸ごと止まります。
以下を提案します。

| 段階 | やること | 拡張のポチ |
|---|---|---|
| **0** | 疎通確認だけ（下記）。0件なので何も書き込まれません | 継続 |
| **0.5** | §4.6 の検証（`date_type` / 3ヶ月上限）→ 結果を共有いただき毎時の設計を確定 | 継続 |
| **1** | **ジョブB（全量）を手動実行で1回** → Dent側で行数と金額を突合 | 継続 |
| **2** | ジョブB を毎朝8時で自動化。**拡張のポチも継続**（冪等なので二重でも壊れません） | 継続 |
| **3** | **ジョブA（毎時）を追加。** まず日中だけ等、狭い時間帯から始めても構いません | 継続 |
| **4** | 1週間ズレが出なければ拡張を停止 | **停止** |
| **5** | 異常検知のD1移行が済んだら Sheets 経路を撤去 | — |

**毎時（ジョブA）は最後に足してください。**全量が正しく入ることを先に確認した方が、
ズレが出たときに切り分けられます。段階2で二重に走っても問題ありません。同じ Airhost が源で、
同じ主キーで upsert されるだけです。

### 毎時実行で必要な実装

1. **多重起動ロック** — 朝のジョブBは10〜15分かかります。前回が走っている間に
   次が始まらないようにしてください。重なっても壊れはしませんが、Airhost への
   同時アクセスが増えるので避けたいです
2. **429 / 5xx が出たら指数バックオフして、その回は諦める** — リトライで叩き続けるのが
   一番危険です。次の時刻に任せてください（upsert なので取りこぼしは自然に埋まります）
3. **セッション切れの検知** — 深夜にログインセッションが切れると朝まで失敗し続けます。
   失敗通知（下記）が効いてきます

**段階0の疎通確認**（トークンが届いたらこれだけ先にお試しください）:

```bash
curl -X POST https://dent-data-api.gen-ishii.workers.dev/cs/ingest/daily-revenue \
  -H "Authorization: Bearer <お渡ししたトークン>" \
  -H "Content-Type: application/json" \
  -d '{"rows":[]}'

# 期待: {"accepted":0,"upserted":0,"changes":0,"orphans":0,"orphan_samples":[]}
# 401 → トークンが違う / 形式不正
# 403 → 権限追加がまだ反映されていません。ご連絡ください（§5.2）
```

### 通知の粒度

**失敗したら Slack に飛ばしてください。** 静かに止まるのが一番困ります。
ただし**毎時の成功通知は出さないでください** — 1日24件流れると、本当に見るべき
失敗通知が埋もれます。

| ジョブ | 成功時 | 失敗時 |
|---|---|---|
| **A. 毎時（差分）** | **通知しない** | 通知する |
| **B. 朝1回（全量）** | 通知する | 通知する |

毎時の失敗が連続する場合（セッション切れ等）は、**同じ内容を24回流すより
「N回連続で失敗中」にまとめていただける**と助かります。そこはお任せします。

```
✅ Airhost同期完了（全量）
日次データ: 12,345行
予約データ: 6,789行
所要時間: 約13分
```

```
❌ Airhost同期失敗
エラー: <メッセージ>
```

Webhook URL は別途お渡しします。Buddy 側の既存の通知経路に乗せていただく形でも構いません。

---

## 10. ご確認いただきたいこと

1. **Buddy から Airhost のブラウザセッションを使って fetch / WebSocket を張れますか。**
   §4 の手順はブラウザコンテキスト前提です。Playwright 等をお持ちなら
   ページ内で `evaluate` して同じことができます
2. **§4.3 の WebSocket 待機（再接続時の再subscribe込み）は実装可能でしょうか。**
   ここが移植で一番手間のかかる箇所という認識です
3. **§4.6 の検証結果**（`date_type` に何が通るか／3ヶ月上限がAPIにもあるか）。
   **これで毎時ジョブの設計が決まります。**着手の最初にお願いします
4. **毎時＋朝1回のスケジュール実行は Buddy 側で持てますか。** Mac mini の
   スリープ/再起動時の挙動もあわせて教えてください。24時間動かす前提で問題があれば
   （夜間はスリープする等）、稼働時間帯だけの毎時でも構いません
5. **多重起動ロックは実装できますか**（§9）
6. **エラー時の Slack 通知はどこに乗せるのが自然ですか**（既存の Buddy 通知経路 / 専用 Webhook）
7. 想定される工数・スケジュール感

---

## 11. 参考

- Chrome拡張の実装（本書の元ネタ・必要ならお渡しします）
  - `operation/chrome-extension/inject.js` — Airhost API 呼び出し・WebSocket待機・blob取得
  - `operation/chrome-extension/content.js` — 期間分割・レート制限・同期フロー
  - `operation/gas/d1-sync.gs` — D1へのPOST（チャンク分割・`_raw` の作り方）
- Dent側の受け口の実装: `wiki/workers/dent-data-api/src/cs-ingest.ts`（認証・PII破棄）／
  `src/ingest.ts`（列マッピング・物件コード解決・upsert。`/internal/*` と共有）
- 前回の指示書: `operation/外注指示書_Buddy_レビュー抑制連携.md`（`/cs/*` とトークンの考え方）
