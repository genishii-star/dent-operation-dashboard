# 業務委託 指示書: Buddy「レビュー依頼しない」タグ ⇄ Dent API 連携

| 項目 | 内容 |
|---|---|
| 発注者 | Dent Inc.（石井 元 / gen_ishii@dent-inc.com） |
| 受注先 | mybrain.tv（三口 聡之介 様） |
| 対象 | buddy AIワーカー / Buddy Web UI |
| 作成日 | 2026-07-16 |
| 状態 | **仕様提示・着手前**。実装可否とご意見をいただいた上で確定します |

---

## 1. 依頼の要旨

Buddy Web UI に「**このゲストにはレビュー依頼を送らない**」というタグを追加し、
タグのON/OFF時に Dent 側 API を呼んでいただきたい。

**Dent側のAPIは実装・稼働済み**です。Buddy側でやっていただくのは
「タグUIの追加」と「ON/OFF時のAPI呼び出し」の2点です。

### 既存タグ機能への相乗りを想定

2026/06/26 CSミーティング由来の開発リスト **A-7「ゲストのタグ/マーク機能」**
（要注意ゲスト／クレーマー等をCS内で共有するラベル）と同じ仕組みに、
タグ種別を1つ足す形を想定しています。独立した新機能として作る必要はありません。

A-7 が「既存タグ機能がホワイトアウトして使えない」状態で保留中と伺っています。
その解消とセットになる想定です。**別立てで先行実装した方が早い場合は、
そのようにご提案ください。**

---

## 2. 背景（なぜ必要か）

Dentでは Airbnb のレビュー対応を自動化しています。この中に
「**ホストからゲストへの評価を投稿する**」処理があります。

Airbnb では、ホストがゲストを評価すると **ゲストに通知が飛び、
ゲスト側にもレビューを書くよう促されます**。

つまりトラブルのあったゲストに対してこれを投稿すると、
**こちらから悪いレビューを呼び込む**ことになります。

「このゲストは評価を投稿しない方がよい」という判断ができるのは、
実際にそのゲストと対応したCSだけです。**この情報はAirhostから取得できません。**
そのため、CSが日常的に触れている Buddy 上でフラグを立てていただき、
Dent側のDBに記録する必要があります。

### なぜ Buddy から Dent を呼ぶ向きなのか

逆向き（Dentが投稿直前にBuddyへ問い合わせる）にはしません。

投稿の判定はDent側のGitHub Actions上で自動実行されます。ここでBuddyに
問い合わせる設計にすると、**Buddy稼働環境が一時的に落ちている間、
Dent側の投稿処理が全て停止**します（安全側に倒す設計のため）。

Buddyからフラグを送っていただき、Dent側のDBに保持する形であれば、
Buddy側の状態に関係なく投稿判定が動きます。**Buddyを常時稼働の前提に
しないための設計**であり、Buddyの信頼性を疑っての判断ではありません。

---

## 3. 連携仕様

### 3.1 エンドポイント

| 操作 | メソッド・パス |
|---|---|
| タグON（抑制を登録） | `POST https://api.dent-inc.com/internal/review-exclusions` |
| タグOFF（抑制を解除） | `DELETE https://api.dent-inc.com/internal/review-exclusions/{予約番号}` |
| 現在の一覧（確認用・任意） | `GET https://api.dent-inc.com/internal/review-exclusions` |

> ⚠️ パスは**変更予定**です。Buddy専用の入口（`/cs/*` 等）を別途用意し、
> そちらをご案内します。理由は §4 を参照。**確定したパスをお伝えするまで
> 実装をお待ちください。**リクエスト/レスポンスの形は変わりません。

### 3.2 認証

Cloudflare Access の Service Token をお渡しします。HTTPヘッダに2つ付けてください。

```
CF-Access-Client-Id:     <お渡しします>
CF-Access-Client-Secret: <お渡しします>
Content-Type:            application/json
```

**Service Token は Buddy のサーバー側（Mac mini）から呼ぶ前提でお渡しします。**
ブラウザのJavaScriptから直接呼ぶとシークレットが利用者に露出するため、
その構成の場合は事前にお知らせください。別方式（トークンURL等）をご用意します。

### 3.3 リクエスト（タグON）

```json
POST /internal/review-exclusions
{
  "confirmation_code": "HMQYXEYQEH",
  "created_by": "nakanishi@example.com",
  "reason": "騒音クレームあり",
  "source": "cs-view"
}
```

| フィールド | 必須 | 内容 |
|---|---|---|
| `confirmation_code` | △ | 予約の確認コード。Airhostの「**チャンネル予約ID**」。Airbnbは `HM...` 形式 |
| `airhost_reservation_id` | △ | Airhostの予約ID（数値）。**上記の代わりにこちらでも可** |
| `created_by` | ✅ | 操作したCSの識別子（メール/ユーザー名など。形式は問いません） |
| `reason` | — | 任意。後から「なぜ除外したか」を追うため。あると助かります |
| `source` | — | `"cs-view"` 固定でお願いします（どの経路で入ったかの記録用） |

**△ = `confirmation_code` と `airhost_reservation_id` のどちらか一方が必須**です。
Buddyが保持している方をお送りください。両方あればどちらでも構いません。

### 3.4 レスポンス

**成功（201）** — 照合できた予約を返します。**CSの画面にこれを表示していただけると、
「意図した予約に対してフラグが立ったか」をその場で確認できます。**

```json
{
  "ok": true,
  "confirmation_code": "HM52ECHEAC",
  "scope": "review_of_guest",
  "reservation": {
    "airhost_reservation_id": "20248543",
    "channel": "Airbnb",
    "channel_reservation_id": "HM52ECHEAC",
    "property_code": "BVV",
    "property_name": "BVV",
    "check_in": "2025-12-26",
    "check_out": "2025-12-29"
  }
}
```

**既に登録済み（200）** — 二度押しは冪等です。エラーにはなりません。

```json
{ "ok": true, "already": true, "confirmation_code": "HMQYXEYQEH", ... }
```

**予約が見つからない（400）** — ⚠️ **重要**

```json
{
  "error": "unknown reservation",
  "detail": "その予約が見つかりません。予約番号を確認してください。抑制は登録されていません。",
  "confirmation_code": "HMNOTEXIST",
  "airhost_reservation_id": null
}
```

**このエラーは必ずCSに見えるようにしてください。** 黙って握りつぶすと、
CSは「フラグを立てた」と思っているのに実際には登録されておらず、
**そのゲストに評価が投稿されます**。フラグの立て忘れより危険な状態です。

**その他の400** — `created_by` 欠落、確認コードの形式不正、
チャンネル予約IDを持たない予約（直接予約・手動作成など。レビュー自動化の
対象外のため抑制できません）。

### 3.5 リクエスト（タグOFF）

```json
DELETE /internal/review-exclusions/HMQYXEYQEH
{ "released_by": "nakanishi@example.com" }
```

パス末尾は `confirmation_code` / `airhost_reservation_id` のどちらでも構いません。
削除ではなく「解除」として記録が残ります（誰がいつ外したかを追跡するため）。

### 3.6 タグの意味（scope）

現状の実装では、抑制されるのは「**ホスト→ゲストの評価投稿**」のみです。
**ゲストが書いた口コミへの返信は止まりません**（既に公開された口コミへの
応答であり、ゲストを新たに促す効果がないため）。

「一切接触しない」まで必要な場合は `"scope": "all"` を付けてください。
通常は不要です。

---

## 4. パスが変更予定である理由（セキュリティ）

現在のAPIは `/internal/*` 配下にあり、ここには**43本のエンドポイント**が
同居しています。この中には Airbnbのセッション情報・2FAコード・全予約データ・
物件マスタの編集など、**レビュー抑制とは無関係かつ機微なものが含まれます**。

Cloudflare Access は現状これらをまとめて認可するため、`/internal/*` の
Service Token をお渡しすると、**タグ機能に必要な権限を大きく超えた
アクセス権をお渡しすることになります**。

三口さん・mybrain.tv様を信用していないという話ではありません。トークンは
どのような運用でも漏洩リスクがあり（環境変数・ログ・端末・将来の担当変更）、
**万一漏れた際の影響範囲を「レビュー抑制リストの改ざん」に限定しておくのが、
双方にとって健全**という判断です。

そのため Buddy専用の入口を切り、**抑制の登録・解除だけができるトークン**を
お渡しします。パスが確定次第ご連絡します。

---

## 5. Dent側の実装状況

| 項目 | 状態 |
|---|---|
| DBテーブル（`review_exclusions`） | ✅ 稼働中 |
| 登録/解除/一覧 API | ✅ 稼働中 |
| 予約実在チェック（誤った予約番号を400で弾く） | ✅ 稼働中 |
| 投稿直前の抑制判定 | ✅ 稼働中 |
| Buddy専用パス + 専用トークン | ⬜ 未（§4） |

**Buddyから送っていただければ、その時点で投稿が止まる状態まで出来ています。**

---

## 6. ご確認いただきたいこと

1. **Buddyは予約の確認コード（Airhostの「チャンネル予約ID」）または
   Airhost予約IDを保持していますか。** どちらか一方があれば連携できます。
   どちらも無い場合はご相談させてください。
2. **A-7タグへの相乗りで実現可能でしょうか。** 別立ての方が早い等あればご提案ください。
3. **API呼び出しはBuddyのサーバー側から行えますか**（§3.2）。
4. 想定される工数・スケジュール感。

---

## 7. 参考

- Dent側の設計: `operation/review/DESIGN_review_exclusions.md`（社内資料。必要なら共有します）
- 関連する開発リスト: `buddy/dev-list-2026-06-26.md` A-7「ゲストのタグ/マーク機能」
