# 業務委託 追加依頼: 予約データに「ゲスト名」を1列足してください

| 項目 | 内容 |
|---|---|
| 発注者 | Dent Inc.（石井 元 / gen_ishii@dent-inc.com） |
| 受注先 | mybrain.tv（三口 聡之介 様） |
| 対象 | buddy AIワーカー（Mac mini 常駐側） |
| 作成日 | 2026-09-02 |
| 関連 | `外注指示書_Buddy_Airhostデータ自動取得.md` §6.1 / §6.2.1 |
| 状態 | **Dent側は受け入れ実装済み。送っていただければそのまま入ります** |

---

## 1. お願いしたいこと（これだけです）

`POST /cs/ingest/reservations` に送っていただいている予約データに、
**`ゲスト名` を1列足してください。**

```diff
  予約サイト / チャンネル予約ID / AirHost予約ID / チェックイン / チェックアウト /
- 物件名 / 物件タグ / 部屋番号 / 国籍 / 状態 / チェックイン状態 / ...
+ 物件名 / 物件タグ / 部屋番号 / 国籍 / ゲスト名 / 状態 / チェックイン状態 / ...
```

- CSVの `ゲスト名` 列（42列CSVの9列目）をそのまま載せるだけです
- `ゲスト名` が空で `宿泊者名` 列（39列目）に値がある予約があります。
  その場合は `宿泊者名` を代わりに使ってください
- **ジョブA（毎時）・ジョブB（朝8時）の両方**でお願いします

**それ以外に変更はありません。** 送信先URL・認証・チャンク数・冪等性の扱いは今のままです。

---

## 2. 🔴 引き続き送らないでください

```
電話番号 / メールアドレス / ゲストアドレス / コメント / 説明
```

**氏名だけが例外です。** 上記5列は今までどおり送信前に落としてください。
Dent 側でも受信時に破棄する実装が入っています（二重の防御）。

---

## 3. なぜ氏名だけ方針を変えたのか

住宅宿泊事業法（民泊新法）で、オーナーは**宿泊者名簿**の備付けが義務づけられています。
これまでオーナーは Airhost の画面を1件ずつ見て手で書き写していました。

Dent のダッシュボードには予約が全部入っているので、そこから名簿を出せるように
したい、というのが今回の依頼の背景です。

当初（2026-08 まで）は「氏名も持たない」設計にしていました。**持たなければ漏れない**
という考え方です。それを崩す判断なので、Dent 側では以下を約束事として実装済みです。

| # | 約束 | 実装 |
|---|---|---|
| 1 | 明細ファイルに焼き込まない | 名前は都度APIで取る。配信物には入れない |
| 2 | オーナー本人にしか返さない | セッションと物件の持ち主を毎回突合してから返す |
| 3 | 閲覧を全部記録する | 誰が・いつ・何件見たかをログに残す |
| 4 | **13ヶ月で自動削除** | 取り込みのたびに古い予約の氏名をNULLにする |
| 5 | メール・WeChat本文に載せない | 手貼りの経路を作らない |

**つまり氏名は「Airhost → Buddy → Dent の D1 → オーナー本人の画面」だけを通り、
それ以外の場所には出ません。** 13ヶ月を過ぎたぶんは自動で消えます。

---

## 4. 対応前でも壊れません

送っていただかなくても取り込みは今までどおり通ります（氏名が空のまま入るだけ）。
エラーにはなりません。

ただし現状は**氏名が1件も入ってこない**ため、名簿機能が使えません。
2026-09-02 に Dent 側で Chrome拡張から手動で16,113件ぶんを流し込みましたが、
これはあくまで一度きりのスナップショットで、**今後入る新規予約には氏名が付きません。**

Buddy 側で対応いただけると、この手作業が不要になります。

---

## 5. 動作確認

送信を始めていただいたら教えてください。Dent 側で以下を確認して結果をお返しします。

- 直近月の予約に対して氏名が何％入ったか
- 氏名以外のPII（電話・メール等）が混ざっていないか

**もし混ざっていた場合も Dent 側で破棄しますが、`notes` フィールドでお知らせします。**
送信経路とログに載せたくないので、Buddy 側でも落としていただけると助かります。

---

## 6. 期限

**急ぎではありません。** 対応いただける時期の目安だけ教えてください。

ご不明点があればいつでもご連絡ください。
